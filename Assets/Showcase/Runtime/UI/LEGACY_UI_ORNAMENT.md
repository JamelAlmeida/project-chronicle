# LEGACY — Showcase UI Ornament (Unreferenced by Live Game)

Hard UI visual reset (2026-08-06): these ornament crops are **no longer loaded** by live presentation.

Live UI uses flat structural `StyleBoxFlat` panels only (`ChronicleUITheme.USE_ORNAMENT_SKINS = false`).
`runtime_texture()` / `showcase_texture()` / `textured_style()` return null for live HUD/windows.

Do **not** re-enable these textures without art-director approval for the replacement HUD pack.

Files remain here (not deleted) so ChatGPT / art pipeline can reference or replace them.
Previous ChronicleV2 / filler HUD assets remain under `LegacyVisuals/UI/`.
